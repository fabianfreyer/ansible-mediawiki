---
instance_name: 'MediaWiki'
subdomain: ''
mediawiki_dir: ''
mediawiki_path: '/srv/{{instance_name}}'
server_name: '{% if subdomain %}{{subdomain}}.{% endif %}{{ansible_fqdn}}'
base_uri: 'http://{{server_name}}/{{mediawiki_dir}}'

admin_user:
  name: root
  pass: admin
mediawiki_db:
  connection: mysql
  name: mw_{{instance_name}}
  user: mw_{{instance_name}}
  password: mediawiki
